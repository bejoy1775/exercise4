name: buildfork
on:
  workflow dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      -uses: actions/checkout@v4
      -name: Cache
       uses: actions/cache@v4.2.3
       with: 
         path: ~/.npm
         key: buildCache

    

          
